#include<stdio.h>
//calculate the area of circle
int main (){
    int radius=6;
  float  a=radius*radius*3.142;
    printf("area of the circle is:%f \n", a);
  //  scanf("%d", &length);
   // printf("enter the breadth of rectangle:\n");
   // scanf("%d", &breadth);
   // printf("the area of rectangle is: %d \n", length*breadth);
    return 0;
}
